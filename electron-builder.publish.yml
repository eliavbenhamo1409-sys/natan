# Used by GitHub Actions for release builds (portable + auto-update)
extends: electron-builder.yml
extraResources:
  - from: .next/standalone
    to: server
  - from: config.production.json
    to: config.json
win:
  target:
    - target: portable
      arch:
        - x64
  publish:
    provider: github
